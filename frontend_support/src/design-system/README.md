# PFS Design System

This folder is a local PFS copy of the PFO `frontend/src/components/ui` primitives for the Support Catalog React P1.
It is intentionally separated from PFO so future changes can be versioned and reviewed before syncing.
