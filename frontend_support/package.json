{
  "name": "plantflow-support-preview",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "PlantFlow Support 프리뷰 B — three.js 파라메트릭 서포트 형상 렌더 (WebView2 호스트)",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
