# 무탭 RS 계열 검증 원장 (2026-07-23~)

> **★★RS1~15 전 타입 종결(2026-07-23)**: RS11~15(+RS12A~D 변형)까지 cycle120에서 완주.
> cycle120 결정타 = standardName 태그접두 해상도(RS12 변형 config 매칭) / 합성 키 "A+A1" 파서 / @formulaA 세로 키 /
> HorizontalAnchor=memberRight(측면부착) / **V-SPLIT(세로 파이프분할)** / VerticalBalloonKeys(스파인 밸룬 옵트인) /
> 치수 오버라이드 0.1 자리. 커밋 `6c33cd8`~`b3f269c`. 잔여 = 격리 결함(별도 사이클)·TR/TRS·FS·SHOE.
>
> **★RS1~10 전 타입 종결(2026-07-23)**: RS1~5(cycle117~118+후속) + RS6~10(cycle119 `49cde0f`+`ad3aead`) 전건 사용자 통과.
> cycle119 결정타 = 양쪽 세로치수(dimVL/dimVR)·RS10 단면가산 575·가로 params 앵커를 파이프 중심으로 전환(RS6 13mm 정렬).
> 잔여 = RS11~15·TR/TRS·FS·SHOE 검수. 미세잔여 = 파이프중심 앵커 전환 후 RC5/RC7/RS4 로그 diff 눈확인 1회(수치 동치 설계라 위험 낮음).

트랙: RS1~15 타입별 검증. 루프 = 사용자 눈검수 코멘트 수집 → 일괄 진단 → 수정 사이클.
선행 종결: GD1~3+RC1~9 전 타입(cycle105~116), cycle104 UI 검증 PASS(`3a0d2ce`).
추출 경로: 팔레트 일괄 선택 추가 → 무탭 추출(검증 완료 경로).

## 사용자 코멘트 (눈검수)

### RS1 (2026-07-23)
1. **BOM QUAN/LEN=645 ≠ 치수 500** — 3D 데이터상 500이 정답. 가로치수는 100+400=500으로 정상.
   - 유사 전례: RC 트랙에서 가로재+플레이트 병합 solid bbox 오염(RC5 700→650, param이 정답).
   - 조사 대상: BOM 행 길이 산출부(BOMs → StandardSupport.RS1?)가 param이 아닌 bbox/카탈로그 값을 쓰는지.
2. **세로치수 75 삭제** — RS1은 세로치수 불요.
   - 처방 후보: `GetNotabTypeConfig`에 RS1 행 추가, vertical=`none`(RC7/8과 동일 모드).

### RS2 (2026-07-23)
1. **세로치수 75 삭제** — RS1과 동일 처방(config vertical=`none`).
2. **F2 밸룬 좌측 31mm 이동** — 대각재(F2) 옆으로 근접. 스크린샷상 F2가 대각재 우측 허공에 떠 있고
   리더가 부재에 닿지 않아 보임. RC7 전례(대각재 앵커 `isRc7DiagonalMember` 분기) 참조.
   - 수동 캘리브레이션 워크플로우 후보: 노브로 확정 후 config 승격.
- 가로 944.6/144.6 소수점 값 = **실제 데이터 값, 정상 인정**(사용자 확정 2026-07-23). 교정 대상 아님.
### RS3 (2026-07-23)
1. **세로치수 50→500** — 부재 단면 F(50) 폴백의 재림(RC4/5 근인과 동일: `GetNotabTypeConfig`에 RS3 행 부재).
   사용자 표기 위치 = 세로재 전장 스팬(상단 가로재~하단). 처방: config 행 추가(param/F2류/vertical), 파라미터 키는 덤프로 확인.
2. **F2 밸룬 이동 X -118, Y +48.5** — 세로재 옆 중단부로. 사용자가 F2를 세로재 중앙에 손표기.
   RS2 F2(-31)와 함께 세로재/대각재 밸룬 앵커 계열 결함. 세로재 밸룬 = 포트 기반 배치 확인 필요.
### RS4 (2026-07-23) — 부재코드 C10(찬넬)
1. **가로치수 900→800** — 사용자 표기 위치의 800이 정답. RC5 700→650 전례(병합 solid bbox 오염 vs param) 의심.
   분할 300/600은 표시 중 — 합 900 ≠ 정답 800: 분할·전장 산출원 불일치 확인 필요.
2. **세로치수 100→500** — config 폴백 계열(RS3 50→500과 동일 패턴, 100은 단면/부분값).
3. **F2 밸룬 이동 X -104, Y -30** — 세로재 옆으로. RS2(-31)·RS3(-118,+48.5)와 동일 계열.
4. **파이프 콜아웃 좌/우 반대** — 사용자: "좌측으로 나와야 하는데 우측으로 나왔음".
   ※스크린샷상 텍스트는 좌상단으로 보임 — 진단 시 `callout-draw` 로그의 `requiredSide/side/viewportCenterX`로
   실측 대조해 어느 쪽 규칙(R1 중앙선)이 틀어졌는지 확정할 것.
### RS5 (2026-07-23) — 門형(패밀리 C), RESTING STRUCTURE NO 5
1. **BOM 테이블 누락** — 표 자체가 없음.
2. **부재 밸룬 누락** — F1/F2 밸룬 없음. 대신 구형 텍스트 콜아웃(L-65×65×6)만 표시
   (= BOM 행 없음 → 밸룬 대체 비활성 → member-text 폴백 경로로 추정, `member-text skip` 조건 역).
3. **세로치수 65→500** — 단면 폴백 계열(65=L-65 단면).
- **★사용자 진단: 파이썬 스크립트/데이터 문제 가능성** — Part Geometry 실측: Dn=80, Bl=16, A=400, A1=400,
  **Ha/Hb 공백**, P1=0, TY=-1. **SupportParams에 F2/F3 없음** → StandardSupport.RS5()가 BOM 행을 못 만들어
  표·밸룬이 연쇄 누락됐을 개연성. C# 결함이 아니라 카탈로그 Python(@activate)/모델 데이터 소관일 수 있음.
  - 분기: (a) Ha/Hb를 모델에서 채우면 해소되는지 먼저 확인 (b) 스크립트가 F2/F3을 아예 안 쓰면 RS5() 매핑 재설계.
  - 참고: 빈 BOM 무음 실패 전례(DesignStd 하드코딩 사고) — 무음 누락에 진단 로그 필요.

## 관찰 메모
- RS1 밸룬 F1=A7 정상 표시, U볼트 UB-012 콜아웃·라인넘버/BOP 콜아웃 정상으로 보임(스크린샷 기준).

---

## cycle 117 라이브 검수 (2026-07-23, `8bf878d` 후)
- 로그 판독: RS1/3/4/5 전 항목 + 회귀 3종(RS12A·RC5·GD1) PASS. RS2는 재추출분에서 세로치수 삭제 확인.
- **RS1**: 이상없음(사용자 판정 종결).
- **RS2**: 세로치수 삭제 ✅. F2 밸룬 X **-31.5** 이동 필요(대각재, 계획 범위 밖 후속분).
- **RS3**: 세로 500 ✅, F2 밸룬 포트 기반 전환 성공(세로재 좌측 인접). 잔차 = X **+30** 이동 요청.
- **RS4**: 가로 800 ✅·세로 500 ✅. 잔여 3건 — ①**분할 방향 반대**: 도면 divider·파이프콜아웃 앵커가 pipeCenterX=385.5(=좌측 600 지점)인데
  실제 파이프는 좌측 200 지점(RS4.py: 부재 스팬 -A1..A, 원점=파이프 → 좌=A1=200/우=A=600). `TryGetNotabRcHorizontalParams`의
  `right=A1` 일반규칙이 RS4 투영과 반대 → **RC7식 소비부 스왑을 RS4에도 적용**(계획서 예고 contingency 발동).
  치수 divider·파이프 콜아웃 앵커가 같은 값을 소비하므로 스왑 1건으로 ②콜아웃 위치·②치수 중심 동시 해소 전망.
  ③F2 밸룬 X **+36** 이동.
- **RS5**: BOM 3행·F1~F3 밸룬·가로 800(400/400) ✅. 잔여 1건 — **세로 500 치수선 스팬 상이**:
  값은 500(Ha)인데 치수선 기하가 legacy bbox 스팬(268.9~389.1=실601)이라 부재(다리) 실장과 불일치.
  요구 = 치수선이 다리 부재 크기를 표시(다리 하단 baseY에서 Ha×scale 위까지, 상단=가로재 하단/윗면 정합은 집도 시 .py psHa 스팬 대조).
  ※주의: port 앵커 게이트는 `VerticalParamKey=="F2"` 하드코딩이라 RS5(Ha)는 미발동 — 철회했던 F2 완화의 재검토
  또는 param 모드에서 스팬=baseY+param×scale 클램프 중 택1(후자가 국소적).
- → 잔차 사이클 방향: 타입별 부재 밸룬 오프셋 config 통로(RC5 PipeCalloutDx/Dy 승격 패턴) + RS2=-31.5·RS3=+30 출하값.

## cycle 118 후속 (2026-07-23 오후)
- `e8b620e`(오프셋+스팬+스왑) 라이브: RS3 +30·RS4 +36·RS4 split(200,600)·RS5 스팬 로그 PASS. RC5/RC7/GD1 무회귀(사용자 판정).
- RS2 -31.5는 `box:support` 과차단 → `e7aa583`+`00fabc9`(support bbox 면제+부호 로그, ★e7aa583은 빌드오류 채 커밋된 사고 — 빌드·커밋 체인 금지 교훈) → 이동은 됐으나 **리더 2.3mm 붕괴**(앵커=bbox 끝 스냅 430과 겹침).
- **RS2 종결**(`59af654`): F2를 RC7식 대각재 포트 앵커 경로(isRc7DiagonalMember)로 전환+오프셋 config 제거. 사용자 "수정 됐음".
- RS3/RS4 "이동 안 보임" 미제: 로그·저장은 이동판(305.1/311.1). ★Details DWG가 **Fasoo DRM 암호화**(accoreconsole 판독 불가 실측) — DRM 복호화 캐시가 구판을 서빙할 가능성. 판정 = F2 원 Properties Center X (305.1=신판 / 275.1=구판 캐시).

## RS6~15 검수 (2026-07-23 오후~)

### RS6 (門형+B1, 사용자 코멘트) — ★RS5도 동일 규칙 적용(재개방)
1. **세로치수 65→500** — RS6 config 행 부재(fheight 폴백, 65=L-65 단면). RS5식 param(Ha) 행 추가.
2. **가로치수 930→800** — bbox 오염(930=A+A1(800)+다리 단면 2×65). 파란 표기=가로재 실장(A+A1)만.
   → RS6(그리고 RS5도) 가로=params(A+A1) 전환. 분할도 465/465→400/400.
3. **F1(가로재) 밸룬이 세로재에 붙음** — horizontal-span 끝단 스냅이 bbox 끝(=세로재 외측)이라 화살표가
   세로재에 닿음. F1 앵커 스팬=가로재 실구간(params) 기반으로 교정 필요(밸룬 스팬 소스 코드 확인).
4. **★신규 규칙: 다리 2개 타입은 세로치수 좌·우 양쪽 표시** — Ha/Hb 각각(좌=Ha, 우=Hb).
   사용자: "F1,F2(다리 param이 F계열)인 타입도 동일" → RS12 계열(F2/F3) 등 門형·걸침보 일반 규칙로 설계.

### RS7 (캔틸레버+하향 다리)
1. **세로치수 75→500** — config 행 부재(fheight 폴백). 세로 부재 실장=500 → param 행(키는 덤프 확인, F2 예상).
2. **F2 밸룬 허공** — 세로재 밸룬이 앵커 미접속 부유. RS3/4식 `MemberAnchorSide="vertical"`(포트 기반)+VLeaderExt=0 처방 예상.

### RS8 (캔틸레버+상향 다리) — RS7과 동일 2건
1. **세로치수 75→500** — config 행 부재. param 행 추가.
2. **F2 밸룬 허공** — RS3/4식 세로재 포트 앵커 처방. (상향 다리라 S2 방향은 port-S2(up) 예상)

### RS9 (T형, 하향 중앙 다리) — RS7/8과 동일 2건
1. **세로치수 65→500** — config 행 부재(65=단면). param 행 추가.
2. **F2 밸룬 허공** — RS3/4식 세로재 포트 앵커 처방.

### RS10 (십자형, 관통 세로재) — RS7~9 패턴 + 값 특이
1. **세로치수 75→575** — config 행 부재. ★값이 500이 아니라 **575 = F2+단면폭(75)** —
   BOM에 선례 있음(BOMs.cs label_67의 RS10 분기: `num94 += DetailProfile 폭, BI!=210일 때`). 치수도 같은 규칙 필요.
2. **F2 밸룬 허공** — RS3/4식 세로재 포트 앵커 처방.

### RS11 (수평바+U볼트 — 파이프축 뷰라 부재가 세로 투영)
1. **세로치수 50→550 (부재 실장)** — fheight 폴백(50=단면). BOM 선례: label_56 RS11 분기 F1=A+A1 →
   550=A+A1 추정(param 덤프로 확정 필요). 단일 키가 아니라 **A+A1 합성값** — config 신모드 또는 RS10식 가산 통로 검토.

### RS12A (ㄷ자형, param={A=250,A1=250,A2=150,F2=500,B1})
1. **가로 601→500(부재)** — bbox 오염(601=500+U볼트/파이프 101). 부재=A+A1=500(BOM label_71 F1과 동일).
   → params 전환 후보. ★단 파이프가 우측 스파인에 측면 부착이라 파이프중심 앵커의 left/right 산정 재검토 필요
   (기존 split guard가 skip이었던 타입 — 로그 12:34 참조, `.py` 대조 필수).
2. **세로 100→600(부재)** — fheight 폴백. 정답 600 = **bbox 전체 높이**(기존 로그 V=600) → `VerticalMode="full"`(GD3 선례) 후보.
3. **F1·F2 밸룬 허공** — 오염된 bbox 스팬 끝단 스냅. 가로 params 정합 시 자동 해소 기대(cycle119 P4 선례).

### RS12B (세로판+우측 가로재, param={A=250,A1=250,A2=150,F2=500,TY=-2})
1. **파이프·U볼트 콜아웃 리더 과신장** — 좌상단 멀리까지 뻗음. 배치 비용/장애물 상황 진단 필요(bbox 오염 정합 후 재평가).
2. **세로 100→500(부재)** — fheight 폴백 → param F2=500.
3. **가로 601→500(부재)** — bbox 오염 → params A+A1=500.
4. **F1 밸룬 허공** — 스팬 정합 시 자동 해소 기대.

### RS12C (수평바 하부 U볼트)
1. **세로치수 100 삭제** — config `VerticalMode="none"` (RS1/2 기성 처방). 가로 444.6/300/144.6은 무지적(정상 인정 추정).
   ※기존 관찰: RS12C BOM rows 0 — BOM 결함은 밸룬 부재로 이어질 수 있으나 사용자 F1 밸룬 표시됨(스크린샷). 진단 시 rows 재확인.

### RS14 (수평바+U볼트)
1. **세로치수 50 삭제** — config `VerticalMode="none"`. 가로 500(250/250) 정상.

### RS15 (벽브래킷+대각재)
1. **세로치수 100→600(부재)** — fheight 폴백(100=단면). 파란 표기=세로재 전장. param 키는 덤프로 확정(F2=600 추정,
   bbox V=701이라 param≠bbox — param 클램프 경로 필요). 가로 603/303/300·F1~F3 밸룬은 무지적.

### RS12D·RS13 — **형상 혼입으로 리뷰 불가 (격리 결함)**
- 3D에서 두 서포트가 같은 파이프 3본을 인접 위치에서 잡고 있음(테스트 모델 배치).
- 추출 도면에 **서로의 부재가 섞여 나옴**(RS12D 도면에 RS13의 V형 부재 등). 치수·밸룬 대상도 오염.
- 근인 후보: 수집 단계가 선택 서포트 외 **같은 파이프에 붙은 이웃 서포트까지 끌어옴**
  (AutoIncludeRelatedParts 계열 또는 클립 볼륨). 실무에서도 인접 서포트는 흔함 → **제품 결함으로 등재**,
  수집부에서 "선택 서포트 + 그 부속(UB)만" 필터 필요.
- 검수 우회: 테스트 모델에서 두 타입을 이격 배치 후 재추출하면 개별 리뷰는 가능.

### RS12D (ㄱ자형, 이격 후 재검수 2026-07-23)
1. **세로치수 → 600(부재)** — 현 도면 표시 100(fheight 폴백). 사용자 표기 "500→600"은 param 덤프로 정합 확인
   (세로재 실장 600 = F2(500)+단면(100)? RS10식 가산 후보 — 덤프·`.py` 대조로 확정).
2. **가로치수 601→445(부재, 현재 허공 지향)** — 445≈444.6 = RS12C와 동일 값 = label_66 공식(A+파이프/2+100)과
   기하 일치 추정(RS2 선례: 공식==기하). bbox 오염 601 → 파이프중심/공식 기반 스팬으로.
3. **F1·F2 밸룬 허공** — 스팬 정합 시 자동 해소 기대.

### ★격리 결함 전역 확정 — RC1 재현 (2026-07-23)
- 종결됐던 RC1 재추출에서 이웃 서포트 형상 + 타 서포트 U볼트(UB-002/003 동시) 혼입.
- 모델에 서포트가 늘며 같은 파이프의 이웃이 수집에 끌려옴 = RS12D×RS13과 동일 근인.
- **cycle 120 최우선 후보로 승격** — 격리가 안 되면 이후 모든 타입 검수·회귀가 오염됨.

### RS12A 재검수 2차 (2026-07-23 오후, fc26c96 빌드)
- **값 재해석 확정**: 가로=**F2(500, 가로재 실장)** — A+A1(600) 아님. A/A1은 **세로 스파인의 파이프 분할**(A=450/A1=150).
- 잔여 3건: ①가로 600→500(param 소스=F2) ②★세로치수 **파이프 중심 분할**(A/A1) 표시 — 세로 분할 신규 기능
  ③F3(하부 가로재) 밸룬 허공 — 포트 앵커 진단.

### RS13 (U형 상향 다리, 이격 후 재검수)
1. **세로치수 75→800(부재 다리, 현재 허공)** — fheight 폴백. 다리 param(F2 계열, 덤프 확정) 클램프로.
2. **★우측 다리에도 800 표시** — cycle119 양쪽 세로치수 통로(`VerticalParamKey2`) 그대로 적용(키=F3 계열).
- 가로 500(250/250)·F1~F3 밸룬 무지적.

### 실측 param (support params dump)
| 타입 | params | BOM rows |
|---|---|---|
| RS1 | A=500 (F2 없음) | 1 |
| RS2 | A=800, F2=900 | ? |
| RS3 | A=600, A1=200, F2=500 | 2 |
| RS4 | A=600, A1=200, F2=500, BI=210 | 2 |
| RS5 | A=400, A1=400, **Ha=∅, Hb=∅** | **0** |
| RS6 | A=400, A1=400, **Ha=∅, Hb=∅**, B1=0 | **0** |

### 계열 A — 치수: RS는 param 경로 진입 자체가 안 됨 (근인 확정)
- 전 RS 로그 `dimH src=fallback=legacy`, `dimV fallback=not-rc`, `vMode=fheight`.
- `GetNotabTypeConfig`(Commands.cs:4910)에 **RS 행이 하나도 없음** → 기본 `fheight`(단면 F) 폴백 = RS1 75/RS2 75/RS3 50/RS4 100/RS5 65.
- 가로 게이트 `rcMemberGeometry`(3559~)도 RC 하드코딩 → RS4는 병합 solid bbox 900(wcsDims=900 실측)이 그대로 치수화. param A+A1=800이 정답.
- **단, RS2는 bbox 944.55가 정답**(사용자 확정) — RS2 기하 자체가 A+pipe/2+100=944.55로 지어짐. → 가로 소스는 타입별 지정 필요(RS4=params, RS1/2/3=legacy 유지).

### 계열 B — F2 밸룬: horizontal-end 규칙에 강제 스냅 (근인 확정)
- RS3: `balloon-draw key=F2 rawAnchor=(255.5,268.9)[=S2 포트, 정답 위치] → anchor=(415.5,268.9)` — `geom=horizontal-span`이
  가로 스팬 우측 끝단으로 이동시킴. RS4 동일(rawAnchor 255.5,379 → 425.5,379).
- 세로재 분기(`verticalPortGate`, 3758)가 config `MemberAnchorSide="vertical"`+`VerticalParamKey="F2"`를 요구 → RS 행 부재로 미발동.
- rawAnchor는 이미 정답(S2 포트) — config 행만 넣으면 RC와 같은 세로재 배치로 전환될 구조.
- 사용자 이동값: RS2 X-31 / RS3 X-118,Y+48.5 / RS4 X-104,Y-30 → 포트 기반 전환 후 잔차만 캘리브레이션.

### 계열 C-1 — RS1 BOM 645 (근인 확정)
- BOMs.cs `label_56`(1462): `F1 = ceil(A + PipeSize(Dn)/2 + 100)` = 500+44.55+100 → **645**. 공식이 낳은 값.
- RS1 기하 실측: 부재 solid=500(wcsDims 500×75×75) — python은 A만 사용. 공식·기하 불일치.
- 대조: RS2는 기하도 944.55(=A+pipe/2+100)라 같은 공식이 **맞음**. → RS1만 F1=A로 교정(GD1·RS11 공유 분기라 국소 조건 필수).

### 계열 D — RS5/RS6 BOM 전멸 (근인 확정, 데이터 아님)
- 10:01 재추출: 사용자가 **Ha=500/Hb=500 채웠는데도 rows 0** → 데이터 문제 기각.
- BOMs.cs `label_71`(1563): `SupportParams["F2"]`·`["F3"]` 요구 — RS5/6 param에는 **F2/F3 키가 없음**(Ha/Hb뿐)
  → KeyNotFound 예외 → 상위 무음 삼킴 → rows 0 (빈 catch성 무음 실패, 진단로그도 없음).
- 처방: RS5/6 분기 = F1=A+A1, F2=Ha, F3=Hb + FrameBOM 예외 시 FileDiag 로그 의무화.
- **★원본 .py 대조 확정(2026-07-23)**: RS5.py `@param`=Dn/BI/A/A1/**Ha/Hb**/P1/TY — F2 선언 자체가 없음(다리 좌=Ha 우=Hb).
  F1은 전 타입 공통 `A+A1` 파생 지역변수(파라미터 아님). RS5.py 77행 `psF2` 변수명·RS4.py 주석 "For F2"가
  개명 잔재 = C# BOMs의 F2/F3 기대는 **구 리비전 파라미터명**(R4 개정 전) 흔적. F5 처방과 스크립트 실체 일치.
  Ha/Hb 모델 공백의 출처 = .acat ParamDefinition 행(py 기본값 500은 acat 값에 밀림).
- **★공백 근인 최종 확정 + 교정 완료(2026-07-23)**: .acat·.pspc의 `ContentGeometryParamDefinition`이
  구명 `F2=500,F3=500` 스냅샷(개명 전 등록분). 배치 시 이 문자열이 파라미터를 공급하는데 현행 @param(Ha/Hb)과
  이름 불일치 → 값 버려지고 공백. **카탈로그 에디터 저장은 이 문자열을 재생성하지 않음**(실측: 저장 후에도 Ha 행 0).
  → SQLite 직접 UPDATE로 RS5/RS6만 F2→Ha, F3→Hb 치환(acat 39행·pspc 38행, 백업 .bak_20260723,
  RS12A/RS13/RC9는 F2/F3 정당 사용이라 WHERE 제외·오폭 0 검증). 신규 배치 Ha/Hb=500 확인 대기.
- RS5 세로치수 500 = Ha. 단 `verticalPortGate`가 `VerticalParamKey=="F2"` 하드코딩(3759) → 키 일반화 필요.

### 부수 관찰 (RS6~15 미코멘트분, 사용자 검수 대기)
- RS12A~D·RS12 계열 BOM rows 0 (BOMs.cs에 RS12 case 부재 추정) — RS6~15 코멘트 수집 시 함께.
- RS4 파이프 콜아웃: 로그 `requiredSide=left side=left` — R1 규칙대로 좌측 배치. **사용자 확정(2026-07-23): 현 로직 유지, 교정 대상 아님.** RS4 항목 4 종결.
